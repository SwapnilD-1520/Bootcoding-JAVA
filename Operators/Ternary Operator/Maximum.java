//Implement a program to find the maximum of two numbers using the ternary operator.

class Maximum
{	
	public static void main(String args[])
	{	
	 	int p = 2;
		int q = 4;
		String result = p > q ? "Maximum"  : "Minimum" ;
		System.out.println("Number is = "+ result );
	}	
}